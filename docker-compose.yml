

services:
  omnisense:
    build: .
    container_name: omnisense
    ports:
      - "8081:8080"      # Web UI (TCP, 외부 포트는 필요에 따라 변경)
      - "5004:5004/udp"  # RTP 입력 (UDP)
    restart: unless-stopped
    environment:
      - HTTP_ADDR=:8080
      - UDP_HOST=0.0.0.0
      - UDP_PORT=5004
